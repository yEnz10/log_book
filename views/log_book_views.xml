<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="log_book_action" model="ir.actions.act_window">
        <field name="name">Log Book</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">log.book</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new sale!
            </p>
        </field>
    </record>

    <!-- Top menu item -->
    <menuitem id="log_book_root"
        name="Log Book"
        sequence="10"/>

    <menuitem id="log_book_menu_root"
        name="Level2"
        parent="log_book_root"
        action="log_book_action"
        sequence="10"/>
    
</odoo>